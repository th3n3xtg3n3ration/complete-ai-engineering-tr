# Alıştırmalar

1. `BankAccount` sınıfı yaz; negatif para çekimini engelle.
2. `Rectangle` ve `Circle` için ortak `area()` sözleşmesi oluştur.
3. Kalıtımla yazılmış bir örneği composition kullanarak yeniden tasarla.
4. Immutable bir `EmailAddress` değer nesnesi geliştir.
5. `Logger` protocol'ü ve iki farklı gerçekleştirim yaz.
6. `Order` sınıfına ürün silme davranışı ekle.
7. Sabit tutarlı indirim politikası geliştir.
8. Ücretsiz kargo politikasını ödeme servisinden ayır.
9. Liskov ilkesini ihlal eden bir örnek yazıp düzelt.
10. Büyük bir arayüzü iki küçük protocol'e böl.
11. Bir repository bağımlılığını constructor injection ile ver.
12. Örnek sipariş sistemine en az beş yeni test ekle.

## Düşünme soruları

- Her sınıf neden bir domain varlığını temsil etmek zorunda değildir?
- Composition hangi durumlarda inheritance'tan daha güvenlidir?
- Python'un duck typing yaklaşımı polymorphism ile nasıl ilişkilidir?
