# Mülakat Soruları

1. SQL sorgusunda grain kavramı neden önemlidir?
2. Primary key, unique constraint ve foreign key nasıl ayrılır?
3. INNER JOIN ile LEFT JOIN arasındaki farkı iş örneğiyle anlat.
4. JOIN sonrası satır sayısının beklenmedik biçimde artmasının nedenleri nelerdir?
5. `COUNT(*)`, `COUNT(column)` ve `COUNT(DISTINCT column)` nasıl ayrılır?
6. `WHERE` ve `HAVING` ne zaman kullanılır?
7. `NOT EXISTS` ve `NOT IN` arasındaki NULL farkı nedir?
8. CTE ile subquery arasında nasıl seçim yaparsın?
9. Window function neden GROUP BY yerine kullanılabilir?
10. `ROW_NUMBER`, `RANK` ve `DENSE_RANK` farkları nelerdir?
11. Her müşteri için en son siparişi nasıl seçersin?
12. Kümülatif revenue sorgusunu nasıl yazarsın?
13. `LAG` ile hangi ürün veya müşteri analizleri yapılabilir?
14. Window frame neden açıkça yazılmalıdır?
15. SQL injection nedir ve parametreli sorgu nasıl yardımcı olur?
16. Dinamik kolon veya tablo adını güvenli biçimde nasıl ele alırsın?
17. Bir analitik sorgunun çıktısını nasıl test edersin?
18. Transaction rollback hangi durumda kritiktir?
19. Foreign key kontrolü neden SQLite'ta açıkça etkinleştirilir?
20. `EXPLAIN QUERY PLAN` çıktısında neleri ararsın?
21. LEFT JOIN sorgusunu yanlışlıkla INNER JOIN'e dönüştüren yaygın hata nedir?
22. Duplicate anahtarlar JOIN sonucunu nasıl etkiler?
23. Aynı sorgunun deterministik sonuç üretmesi için ne gerekir?
24. Top-N per group problemini nasıl çözersin?
25. SQL ve pandas ile aynı analizi yaptığında eşitliği nasıl doğrularsın?
