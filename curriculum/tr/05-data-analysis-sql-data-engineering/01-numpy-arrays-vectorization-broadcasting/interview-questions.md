# Mülakat Soruları — NumPy ve Vektörleştirme

1. NumPy dizileri neden Python listelerinden daha hızlı olabilir?
2. Vektörleştirme gerçekten döngüsüz müdür?
3. `shape`, `strides` ve memory layout arasındaki ilişki nedir?
4. View ile copy arasındaki fark üretimde nasıl bug oluşturur?
5. Basic slicing ve fancy indexing davranışlarını karşılaştır.
6. Broadcasting kurallarını `(32,128)` ve `(128,)` üzerinden açıkla.
7. `axis=0` ve `axis=1` semantiğini veri tablosu bağlamında açıkla.
8. `keepdims=True` neden faydalıdır?
9. `float32` ve `float64` arasında nasıl seçim yaparsın?
10. Integer overflow ve floating-point precision loss örneği ver.
11. NaN, inf ve missing-value politikasını nasıl tasarlarsın?
12. Sabit feature standardization sırasında nasıl ele alınmalıdır?
13. Min–max scaling ile standardization ne zaman tercih edilir?
14. Validation verisinde preprocessing `fit` etmek neden leakage'dır?
15. Fit/transform sözleşmesinin temel invariant'ları nelerdir?
16. Cosine similarity'yi batch halinde nasıl vektörleştirirsin?
17. Pairwise distance hesabında 3-D geçici diziden nasıl kaçınırsın?
18. `argpartition` ile `argsort` ne zaman tercih edilir?
19. Vektörleştirme neden bazen daha fazla bellek tüketir?
20. Büyük array işlemlerinde temporary allocation nasıl azaltılır?
21. Benchmark sonuçlarının güvenilir olması için ne yaparsın?
22. Warm-up ve tekrar ölçümü neden önemlidir?
23. `np.asarray` ne zaman `np.array`dan daha uygundur?
24. C-contiguous ve Fortran-contiguous diziler performansı nasıl etkileyebilir?
25. `einsum` kullanımının avantaj ve riskleri nelerdir?
26. Online mean/variance ne zaman batch hesaplamadan iyidir?
27. Memory mapping hangi veri boyutlarında anlamlı olur?
28. NumPy kodunda input mutation'ı nasıl önlersin?
29. Pipeline state'ini immutable tutmanın yararı nedir?
30. NumPy'den pandas, Polars veya PyTorch'a geçerken hangi veri sözleşmelerini korursun?
